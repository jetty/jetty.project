[description]
This module enables the Goal oriented Deployer

[tags]
deployment

[provides]
deployer|default

[depend]
server

[lib]
lib/jetty-deploy-${jetty.version}.jar

[xml]
etc/jetty-goal-deployer.xml

