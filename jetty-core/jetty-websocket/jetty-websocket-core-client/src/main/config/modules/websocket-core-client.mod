[description]
Enables the Jetty WebSocket Core Client API support.

[tags]
websocket

[depends]
client

[lib]
lib/jetty-websocket-core-common-${jetty.version}.jar
lib/jetty-websocket-core-client-${jetty.version}.jar
