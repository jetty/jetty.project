[description]
Enables Deployer to apply default configuration to webapp deployments

[depend]
deploy

[xml]
etc/global-webapp-deploy.xml

[ini-template]

# Location of webapp xml config file to apply after context xml
# jetty.deploy.globalWebappConfig=${jetty.base}/etc/global-webapp-config.xml
