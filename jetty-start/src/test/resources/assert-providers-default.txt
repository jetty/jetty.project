## The XMLs we expect (order is important)
XML|${jetty.base}/etc/logging-a.xml

# The LIBs we expect (order is irrelevant)

# The Properties we expect (order is irrelevant)
PROP|logging.prop=a

# Files / Directories to create

